package:
  name: ora
  version: 0.2.4

about:
  home: https://github.com/alexhsamuel/ora
  license: BSD
  license_file: LICENSE

source:
  git_rev: v0.2.4
  git_url: https://github.com/alexhsamuel/ora.git

requirements:
  build:
    - numpy
    - python 3.6.*
    - setuptools
  run:
    - libgcc
    - numpy
    - python 3.6.*

build:
  script: make -j all test-cxx; python setup.py install --single-version-externally-managed --record=record.txt

test:
  requires:
    - pytest
    - pytz
  imports:
    - ora
  commands:
    - pytest --pyargs ora

