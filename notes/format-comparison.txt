  %a NA the weekday abbreviated name
  %b NA the month abbreviated name
- %B %B the month name
- %d %d the one-indexed day of the month
     %D (not implemented)
     %g the last two digits of the week number of the week date
     %G the full week year of the week date
- %j %j the one-indexed ordinal day of the year
- %m %m the one-indexed month number
     %V the one-indexed week number of the year
  %U    the zero-indexed Sunday week number of the year
  %W    the zero-indexed Monday week number of the year
- %w %w the weekday number, 0 = Sunday through 6 = Saturday
* %A %W the weekday name
- %y %y the last two digits of the year
- %Y %Y the full year
- %f %f 6 fractional digits (truncated) of seconds
  %I %h the 12-hour hour number
- %H %H the 24-hour hour number
- %M %M the minute number
- %p %p either "AM", for hour < 12, or "PM", otherwise
- %S %S seconds of the minute
     %T (not implemented)
     %o the total signed time zone offset in seconds
     %q the minutes part of the time zone offset
     %Q the hours part of the time zone offset
*    %U the sign of the time zone offset
     %u the six-character time zone offset, e.g. +00:00
- %Z %Z the time zone name
*    %z the [military time zone](https://en.wikipedia.org/wiki/List_of_military_time_zones) letter
  %z    5-char UTC offset, e.g. +00:00
  %c    locale's date/time representation
  %x    locale's date representation
  %X    locale's time representation
